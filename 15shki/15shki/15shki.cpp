#include "header.h";

void vvod()
{
    cout << "Введите какое число вводить" << endl;
    int x;
    cin >> x; 
}

int move(int x, int** arr)
{
    int a;
    int b;
        for (int i = 0; i < 4; i++)
            {
                for (int j = 0; j < 4; j++)
                    {
                        a.find(x);
                        b.find(0);
                        swap(a, b);
    return ProvProb(int** arr);
    if (!(ProvProb))
    {
        move(arr);
    }
}
